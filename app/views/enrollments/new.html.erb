<div class="container-fluid">

  <div class="col-md-2">
    <%= render 'shared/classes_by' %>
  </div>
  <div class="col-md-8">
    <div class="row text-center">
      <p>
      By clicking this button you, <%= current_user.first_name %> <%= current_user.last_name %> are enrolling in this class. <br>
      You may drop this class before the registration time period ends. <br>
      After the end of the registration peroiod special permission is required to drop this class. <br>
      If there is a schedule conflict, you will not be able to register for this class until the conflict is resolved. <br>
      Keep in mind, if you drop the class and plan on re-enrolling at a later date, your spot will not be saved,<br>
      or there may no longer be seats available in the class.
      </p>
      <%= form_for [@course, @section, @enrollment], class: "form-horizontal" do |form| %>
        <div class="form-group">
          <%= form.submit :'Enroll', class: "btn btn-default" %>
        </div>
      <% end %>
    </div>
  </div>
</div>

<h5>enrollments/new page</h5>