<% department_order.each do |department| %>
  <%= link_to "#{ department.name }",
      department_path(department),
      class: "btn btn-default btn-sm top-buttons dept-btns",
      id: "dept/#{department.id}" %>
<% end %>
